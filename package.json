{
	"name": "fight-class",
	"private": true,
	"type": "module",
	"description": "턴제 1:1 웹 대전 게임 (Cloudflare Workers + Durable Objects)",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "node --test test/"
	},
	"devDependencies": {
		"wrangler": "^4.0.0"
	}
}
